spring:
  datasource:
    url: jdbc:mysql://localhost:3308/gestionstock
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

##Security DBMS
  jpa:
    database: MYSQL
    show-sql: false

#Hibernate DDL Auto (create, create-drop, update)
  hibernate:
    ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8881